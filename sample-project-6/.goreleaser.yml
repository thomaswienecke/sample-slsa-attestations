project_name: sample-project-6
builds:
  - id: "binary"
    binary: sample-project-6-{{ .Os }}-{{ .Arch }}
    env: [CGO_ENABLED=0]
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
    no_unique_dist_dir: true

archives:
  - format: binary
